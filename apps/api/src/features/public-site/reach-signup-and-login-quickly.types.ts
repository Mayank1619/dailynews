export type PublicActionRoute = {
  label: string;
  route: string;
};

export type ReachSignupAndLoginQuicklyRoutes = {
  primary: PublicActionRoute;
  login: PublicActionRoute;
  blog: PublicActionRoute;
};
